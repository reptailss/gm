
import { SwaggerConfig } from 'os-core-ts'

export default function buildSwaggerConfig(): SwaggerConfig {
    return {
        title: 'test5v API',
        description: '-',
        hasAuth: true
    }
}
        